# PsyChat - Streamlit Cloud Compatible (Python 3.13)
# ONLY packages with pre-built wheels (no source compilation)

# Core frameworks (latest versions with Python 3.13 wheels)
streamlit>=1.39.0
pandas>=2.2.0
numpy>=1.26.0

# Azure OpenAI
openai>=1.35.0

# Basic NLP (lightweight)
nltk>=3.8.1

# Visualization (lightweight)
plotly>=5.18.0

# Document processing
python-docx>=1.1.0

# Utilities
tqdm>=4.66.1

# Remove ALL packages that require source compilation:
# - rouge-score (requires compilation)
# - transformers (requires torch compilation)
# - torch (requires compilation)
# - scikit-learn (requires compilation)
# - matplotlib (requires compilation)
# - seaborn (requires compilation)