# PsyChat - Ultra-Minimal Requirements for Python 3.13
# Removes ALL packages that require distutils

# Core frameworks (no distutils dependencies)
streamlit==1.31.0
pandas==2.0.3
numpy==1.24.4

# Azure OpenAI
openai>=1.35.0

# Basic NLP (no heavy dependencies)
nltk==3.8.1

# Visualization (lightweight)
plotly==5.18.0

# Document processing
python-docx==1.1.0

# Utilities
tqdm==4.66.1

# Remove problematic packages that require distutils:
# - transformers (requires torch, causes distutils issues)
# - torch (heavy, causes distutils issues) 
# - rouge-score (may have distutils dependencies)
# - scikit-learn (may have distutils dependencies)
# - matplotlib/seaborn (may have distutils dependencies)